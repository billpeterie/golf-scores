# Web version — GolfScores

Open `web/index.html` in any modern browser. The app stores rounds in `localStorage`.

Commands to open a local static server (optional):

```bash
# from repo root
python3 -m http.server 8000 --directory web
# then open http://localhost:8000
```
